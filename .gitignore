# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local env vars (GCP_PROJECT 등)
.env

# macOS
.DS_Store

# 컴파일된 파이프라인 산출물 — 각자 로컬에서 컴파일해 사용한다.
# (CI/CD 의 경우 IMAGE_TAG 가 매 빌드마다 새로 박히므로 commit 의미 없음)
01-first-pipeline/*.yaml
02-data-sharing/*.yaml
03-ci-cd/*.yaml
